'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';import Link from 'next/link';
export default function Login(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[err,setErr]=useState(''),router=useRouter();async function go(e:any){e.preventDefault();setErr('');const r=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password})});const j=await r.json();if(!r.ok)return setErr(j.error||'Login failed');router.push('/dashboard')}return <main className="mx-auto max-w-md px-6 py-24"><div className="card p-7"><h1 className="text-3xl font-bold">Welcome back</h1><form onSubmit={go} className="mt-6 space-y-4"><input placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} /><input placeholder="Password" type="password" value={password} onChange={e=>setPassword(e.target.value)}/>{err&&<p className="text-red-400">{err}</p>}<button className="btn w-full">Login</button></form><p className="mt-5 text-sm text-zinc-400">No account? <Link href="/signup" className="text-pink-400">Create one</Link> • <Link href="/forgot-password" className="text-pink-400">Forgot password</Link></p></div></main>}

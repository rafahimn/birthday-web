'use client';import {useState} from 'react';export default function Forgot(){const [email,setEmail]=useState(''),[msg,setMsg]=useState('');async function go(e:any){e.preventDefault();await fetch('/api/auth/forgot',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email})});setMsg('If the account exists, reset instructions have been sent.')}return <main className="mx-auto max-w-md px-6 py-24"><div className="card p-7"><h1 className="text-2xl font-bold">Reset password</h1><form onSubmit={go} className="mt-5 space-y-4"><input placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><button className="btn w-full">Send reset email</button></form>{msg&&<p className="mt-4 text-emerald-400">{msg}</p>}</div></main>}

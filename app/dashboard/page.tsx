import Link from 'next/link';import {getSessionUser} from '@/lib/auth';import {redirect} from 'next/navigation';import {db} from '@/lib/db';
export default async function Dashboard(){const u=await getSessionUser();if(!u)redirect('/login');const sites=await db.website.findMany({where:{userId:u.id},orderBy:{updatedAt:'desc'}});return <main className="mx-auto max-w-6xl px-6 py-10"><header className="flex items-center justify-between"><div><p className="text-zinc-400">Dashboard</p><h1 className="text-4xl font-black">Hi, {u.name||u.email}</h1></div><div className="flex gap-2"><Link className="btn" href="/builder/new">+ Create Website</Link><a className="btn2" href="/api/auth/logout">Logout</a></div></header><section className="mt-8 grid gap-4 md:grid-cols-3"><div className="card p-5"><p className="text-zinc-400">Websites</p><b className="text-3xl">{sites.length}</b></div><div className="card p-5"><p className="text-zinc-400">Published</p><b className="text-3xl">{sites.filter(s=>s.status==='published').length}</b></div><div className="card p-5"><p className="text-zinc-400">Total views</p><b className="text-3xl">{sites.reduce((a,s)=>a+s.views,0)}</b></div></section><section className="mt-8"><h2 className="text-2xl font-bold">My Websites</h2><div className="mt-4 grid gap-4 md:grid-cols-2">{sites.map(s=><div className="card p-5" key={s.id}><div className="flex justify-between"><div><h3 className="font-bold">{s.title}</h3><p className="text-sm text-zinc-400">/{s.slug} • {s.status}</p></div><Link href={`/builder/${s.id}`} className="btn2">Edit</Link></div></div>)}{!sites.length&&<div className="card p-8 text-zinc-400">No websites yet. Create your first birthday experience.</div>}</div></section></main>}

'use client';
import {useEffect,useState} from 'react';
export default function Page(){const [items,setItems]=useState<any[]>([]);useEffect(()=>{fetch('/api/admin/templates/spotlight').then(r=>r.ok?r.json():[]).then(setItems)},[]);
async function toggle(t:any){const r=await fetch('/api/admin/templates/spotlight',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({id:t.id,spotlight:!t.config?.spotlight})});if(r.ok)setItems(xs=>xs.map(x=>x.id===t.id?{...x,config:{...(x.config||{}),spotlight:!x.config?.spotlight}}:x))}
return <main className="mx-auto max-w-5xl p-6 md:p-10"><div className="card p-6"><h1 className="text-3xl font-bold">Template Spotlight</h1><p className="mt-2 text-zinc-400">Highlight templates on the public template experience.</p><div className="mt-6 grid gap-3">{items.map(t=><div key={t.id} className="flex items-center justify-between rounded-2xl bg-white/5 p-4"><div><b>{t.name}</b><div className="text-xs text-zinc-500">{t.category}</div></div><button className="btn2" onClick={()=>toggle(t)}>{t.config?.spotlight?'⭐ Highlighted':'Highlight'}</button></div>)}</div></div></main>}

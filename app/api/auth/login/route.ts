import {NextResponse} from 'next/server';import {db} from '@/lib/db';import {verifyPassword,sessionCookie} from '@/lib/auth';import {loginSchema} from '@/lib/validation';export async function POST(req:Request){try{const v=loginSchema.parse(await req.json());const u=await db.user.findUnique({where:{email:v.email.toLowerCase()}});if(!u?.passwordHash||!(await verifyPassword(v.password,u.passwordHash)))return NextResponse.json({error:'Invalid email or password'},{status:401});const r=NextResponse.json({ok:true});r.cookies.set(sessionCookie,u.id,{httpOnly:true,sameSite:'lax',secure:process.env.NODE_ENV==='production',path:'/',maxAge:60*60*24*30});return r}catch{return NextResponse.json({error:'Invalid request'},{status:400})}}
